# LoL Meta Analyzer

Desktop приложение для анализа патчей League of Legends, построенное на **Tauri** (Rust) и **React** (TypeScript).

## 🎯 Возможности

- **Анализ патч-нотов**: Автоматический парсинг изменений чемпионов с официального сайта Riot Games
- **Отслеживание меты**: Сравнение статистики чемпионов между патчами (Win Rate, Pick Rate, Ban Rate)
- **Прогнозирование**: Предсказание влияния изменений на мету на основе текстовых изменений из патч-нотов
- **История патчей**: Хранение данных о последних 20 патчах в SQLite базе данных
- **Красивый UI**: Современный интерфейс с темной темой и интуитивной навигацией

## 🚀 Технологии

- **Backend**: Rust (Tauri v2)
- **Frontend**: React + TypeScript + Vite
- **Стилизация**: Tailwind CSS
- **База данных**: SQLite (через sqlx)
- **Парсинг**: scraper (HTML), reqwest (HTTP)

## 📦 Установка

### Требования

- Node.js 18+
- Rust (latest stable)
- Windows 10+ (для сборки .msi инсталлера)

### Разработка

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run tauri dev
```

### Сборка

```bash
# Сборка приложения и создание инсталлера
npm run tauri build
```

Инсталлер будет находиться в `src-tauri/target/release/bundle/msi/`

## 🎮 Использование

1. Введите версию патча (например, `25.23`)
2. Нажмите "Обновить" для парсинга данных с OP.GG и Riot Games
3. Просматривайте изменения на трех страницах:
   - **Патч Ноут**: Текстовые изменения из патч-нотов Riot
   - **Изменения Меты**: Сравнение статистики между патчами
   - **Прогнозы**: Предсказание влияния изменений на мету

## 📊 Структура проекта

```
lol-meta-analyzer/
├── src/                    # React фронтенд
│   ├── App.tsx            # Главный компонент с роутингом
│   └── ...
├── src-tauri/             # Rust бэкенд
│   ├── src/
│   │   ├── lib.rs         # Tauri команды
│   │   ├── scraper.rs     # Парсер OP.GG и Riot
│   │   ├── db.rs          # SQLite логика
│   │   ├── analyzer.rs    # Логика сравнения патчей
│   │   └── models.rs      # Структуры данных
│   └── ...
└── public/                 # Статические ресурсы
```

## 🔧 Настройка

Приложение автоматически создает базу данных `patches.db` в директории приложения при первом запуске.

## 📝 Лицензия

MIT

## 🤝 Вклад

Pull requests приветствуются! Для крупных изменений сначала откройте issue для обсуждения.

## ⚠️ Примечания

- Парсер OP.GG может быть заблокирован при слишком частых запросах (добавлены задержки)
- Структура сайтов Riot Games и OP.GG может изменяться, что потребует обновления парсера
- Для работы парсера требуется стабильное интернет-соединение

